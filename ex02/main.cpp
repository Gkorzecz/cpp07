#include "Array.hpp"
#include <iostream>

int main (void)
{
    // unsigned int n = 2;
    // int *a = new int();
    // a[0] = 1;
    // a[18] = 12;
    // std::cout << a[18] << std::endl;
    return (0);
}